<Window x:Class="MyScheduler.Views.ScheduleEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="일정 입력" Width="420" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="제목" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0,0,8"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="시작" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,8">
            <DatePicker SelectedDate="{Binding StartDate}"/>
            <TextBox Width="70" Margin="8,0,0,0"
                     Text="{Binding StartTimeText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="HH:mm 예) 09:30"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="종료" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,8">
            <DatePicker SelectedDate="{Binding EndDate}"/>
            <TextBox Width="70" Margin="8,0,0,0"
                     Text="{Binding EndTimeText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="HH:mm 예) 11:00"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="장소" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="8,0,0,8"
                 Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="메모" VerticalAlignment="Top"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="8,0,0,8"
                 Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap"/>

        <!-- 에러 메시지 -->
        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Margin="0,0,0,8"
                   Foreground="Crimson"
                   Text="{Binding ErrorMessage}"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="취소" Width="80" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="저장" Width="80" IsDefault="True"
                    Command="{Binding SaveCommand}"
                    Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
</Window>
